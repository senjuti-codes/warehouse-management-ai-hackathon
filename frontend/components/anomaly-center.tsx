"use client"

import { useEffect, useMemo, useState } from "react"
import { AlertCircle, Check, Filter, Menu, Search, ShieldAlert, Sparkles } from "lucide-react"
import { Sidebar } from "@/components/control-tower-dashboard"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"

type LiveAnomaly = { id: number; type: string; severity: string; sheet: string; message: string; recommendation: string; category: string; confidence: number; decision_status?: string }
const apiBaseUrl = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000"

function SummaryCard({ label, value, detail, icon: Icon }: Readonly<{ label: string; value: string; detail: string; icon: typeof AlertCircle }>) {
  return <Card className="border-0 bg-white shadow-sm card-hover"><CardContent className="p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-medium text-slate-500">{label}</p><p className="mt-2 text-2xl font-semibold text-[#17211f]">{value}</p><p className="mt-2 text-xs text-slate-400">{detail}</p></div><Icon className="size-5 text-slate-400" /></div></CardContent></Card>
}

export function AnomalyCenter() {
  const [anomalies, setAnomalies] = useState<LiveAnomaly[]>([])
  const [search, setSearch] = useState("")
  const [severity, setSeverity] = useState("all")
  const [selected, setSelected] = useState<LiveAnomaly | null>(null)
  useEffect(() => { fetch(`${apiBaseUrl}/api/anomalies`).then((response) => response.ok ? response.json() : Promise.reject(new Error("Unable to load anomalies"))).then((rows: LiveAnomaly[]) => setAnomalies(Array.isArray(rows) ? rows : [])).catch(() => setAnomalies([])) }, [])
  const filtered = useMemo(() => anomalies.filter((anomaly) => { const query = search.toLowerCase(); return (!query || [anomaly.message, anomaly.type, anomaly.sheet, String(anomaly.id)].some((value) => value.toLowerCase().includes(query))) && (severity === "all" || anomaly.severity === severity) }), [anomalies, search, severity])
  const critical = anomalies.filter((item) => item.severity === "critical").length
  const high = anomalies.filter((item) => item.severity === "high").length
  return <div className="flex min-h-screen bg-[#f4f6f3] font-sans text-[#17211f]"><Sidebar activeLabel="Anomaly Queue" /><main className="min-w-0 flex-1"><header className="flex h-20 items-center justify-between border-b border-slate-200/80 bg-[#f8faf7] px-5 sm:px-8"><div className="flex items-center gap-3"><Button variant="ghost" size="icon" className="lg:hidden"><Menu /></Button><div><p className="text-xs font-medium uppercase tracking-[0.16em] text-slate-400">Operations workspace</p><h1 className="mt-1 text-xl font-semibold">Anomaly Center</h1></div></div><span className="text-xs text-slate-500">Workbook-backed feed</span></header><div className="mx-auto max-w-[1500px] space-y-6 p-5 sm:p-8"><section><h2 className="text-2xl font-semibold sm:text-3xl">Anomaly Center</h2><p className="mt-1 text-sm text-slate-500">Detected directly from the latest workbook ingestion.</p></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><SummaryCard label="Total anomalies" value={String(anomalies.length)} detail="Current workbook run" icon={AlertCircle} /><SummaryCard label="Critical" value={String(critical)} detail="Immediate attention" icon={ShieldAlert} /><SummaryCard label="High" value={String(high)} detail="Review required" icon={AlertCircle} /><SummaryCard label="Recommendations" value={String(anomalies.filter((item) => Boolean(item.recommendation)).length)} detail="Deterministic actions" icon={Sparkles} /></section><section className="space-y-3"><div className="flex flex-col gap-3 rounded-xl bg-white p-4 shadow-sm sm:flex-row"><div className="relative min-w-0 flex-1"><Search className="absolute left-2.5 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><Input aria-label="Search anomalies" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search workbook anomalies" className="h-8 border-slate-200 pl-9" /></div><Select value={severity} onValueChange={(value) => setSeverity(value ?? "all")}><SelectTrigger className="w-full sm:w-[150px]"><SelectValue placeholder="Severity" /></SelectTrigger><SelectContent><SelectItem value="all">Severity: All</SelectItem><SelectItem value="critical">Critical</SelectItem><SelectItem value="high">High</SelectItem><SelectItem value="medium">Medium</SelectItem></SelectContent></Select></div><Card className="border-0 bg-white shadow-sm card-hover"><CardHeader><div className="flex items-center justify-between"><div><CardTitle>Live anomaly queue</CardTitle><p className="mt-1 text-xs text-slate-400">{filtered.length} of {anomalies.length} records shown</p></div><Filter className="size-4 text-slate-400" /></div></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>Issue</TableHead><TableHead>Source</TableHead><TableHead>Severity</TableHead><TableHead>Recommendation</TableHead><TableHead>Decision</TableHead></TableRow></TableHeader><TableBody>{filtered.map((anomaly) => <TableRow key={anomaly.id} className="cursor-pointer" onClick={() => setSelected(anomaly)}><TableCell><p className="font-medium">{anomaly.message}</p><p className="font-mono text-xs text-slate-400">AN-{anomaly.id} · {anomaly.type}</p></TableCell><TableCell className="text-sm text-slate-500">{anomaly.sheet}</TableCell><TableCell><Badge variant="outline">{anomaly.severity}</Badge></TableCell><TableCell className="max-w-sm text-sm text-slate-600">{anomaly.recommendation}</TableCell><TableCell>{anomaly.decision_status === "approved" ? <Badge><Check className="size-3" /> Approved</Badge> : <span className="text-xs text-slate-400">Pending review</span>}</TableCell></TableRow>)}</TableBody></Table>{filtered.length === 0 && <p className="py-10 text-center text-sm text-slate-400">No workbook anomalies match the current filters.</p>}</CardContent></Card></section></div></main>{selected && <Card className="fixed inset-x-4 bottom-4 z-20 border-slate-200 bg-white shadow-xl card-hover sm:left-auto sm:right-6 sm:w-[420px]"><CardHeader><CardTitle>{selected.message}</CardTitle></CardHeader><CardContent><p className="text-sm text-slate-600">{selected.recommendation}</p><Button className="mt-4" onClick={() => setSelected(null)}>Close</Button></CardContent></Card>}</div>
}
